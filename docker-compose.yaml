version: '3.1'

services:
  postgres:
    image: postgres:latest
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: xoom_schemata
      POSTGRES_USER: xoom_test
      POSTGRES_PASSWORD: vlingo123

volumes:
  xoom_schemata_postgres:
