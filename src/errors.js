const errors = Object.freeze({
	EMPTY: "may not be empty",
	VERSION: "must be a semantic version number (major.minor.patch, e.g. 1.6.12)",
	SUBMIT: "no field should be empty!"
});

export default errors